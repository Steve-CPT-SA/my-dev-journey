Responsive Navigation Bar
    -A simple responsive navigation bar built with HTML and CSS.
    -The navigation automatically adapts between desktop and mobile layouts using CSS media queries.

Desktop
    -Sticky navigation bar at the top
    -Horizontal layout using Flexbox

Mobile
    -Fixed navigation bar at the bottom
    -Responsive layout optimized for smaller screens

Technologies Used
    -HTML5
    -CSS3

CSS Concepts Practiced
    -@media
    -:root
    -position
    -z-index
    -flex-direction
    -justify-content
    -align-items
    -top
    -bottom
    -Flexbox

What I Learned
    -How to create responsive layouts using media query breakpoints
    -How to switch layouts between desktop and mobile views
    -How to use Flexbox for navigation alignment
    -How position: sticky and position: fixed behave
    -How to improve mobile usability with bottom navigation

Features
    -Responsive design
    -Sticky desktop navigation
    -Fixed mobile bottom navigation
    -Clean Flexbox layout
    -Smooth hover effects
    -CSS custom properties using :root

Responsive Behavior
@media (max-width: 760px)

When the screen width becomes 760px or smaller:

The navbar moves to the bottom
position: fixed is applied
Navigation items evenly distribute across the screen
Mobile-friendly spacing is added
Future Improvements
Add icons for navigation items
Add active link styling
Add animations/transitions
Add dark/light mode toggle
Convert into a reusable component
Author

Created by Stephen as part of practicing responsive web design and CSS layout concepts.